# ==========================================
# BACKEND SERVER CONFIGURATION
# ==========================================
NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:5173

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sufi_essences
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_POOL_SIZE=20

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRY=7d

# ==========================================
# FRONTEND CONFIGURATION (Vite)
# ==========================================
VITE_API_URL=http://localhost:5000/api

# Application Environment
VITE_APP_ENV=production
VITE_APP_VERSION=1.0.0

# Development Only (Remove in Production)
# VITE_DIRECT_LOGIN_ENABLED=false
# VITE_DIRECT_LOGIN_DEFAULT_ROLE=customer

# Monitoring (Optional - Only set in production)
# VITE_SENTRY_DSN=your-sentry-dsn-here
# VITE_LOGROCKET_APP_ID=your-logrocket-app-id-here

# Security Settings
VITE_ENABLE_DEBUG=false
VITE_DISABLE_CONSOLE_LOGS=true

# Performance Settings
VITE_ENABLE_PERFORMANCE_MONITORING=false
VITE_BUNDLE_ANALYZER=false

# ==========================================
# DATABASE SETUP INSTRUCTIONS
# ==========================================
# Before using this application, ensure you've completed these steps:
#
# 1. SUPABASE PROJECT SETUP:
#    - Create a new project at https://supabase.com
#    - Copy your project URL and anon key from Settings > API
#    - Update VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY above
#
# 2. DATABASE SCHEMA SETUP:
#    - Run SQL scripts in order from the supabase-scripts directory:
#      a. supabase-scripts/01-auth-schema.sql
#      b. supabase-scripts/02-user-profiles.sql
#      c. supabase-scripts/03-categories.sql
#      d. supabase-scripts/04-products.sql
#      e. supabase-scripts/05-inventory.sql
#      f. supabase-scripts/06-shopping-cart.sql
#      g. supabase-scripts/07-wishlist.sql
#      h. supabase-scripts/08-orders.sql
#      i. supabase-scripts/09-reviews.sql
#      j. supabase-scripts/10-coupons.sql
#      k. supabase-scripts/11-analytics.sql
#      l. supabase-scripts/12-admin-functions.sql
#      m. supabase-scripts/13-security-policies.sql
#      n. supabase-scripts/14-sample-data.sql (optional for testing)
#
# 3. ROW LEVEL SECURITY (RLS):
#    - All tables have RLS enabled with appropriate policies
#    - Test with: SELECT * FROM profiles LIMIT 1;
#    - Should work without errors
#
# 4. AUTHENTICATION SETUP:
#    - Configure auth settings in Supabase Dashboard
#    - Set site URL to http://localhost:5173 for development
#    - Add redirect URLs as needed
#
# 5. USER ACCOUNTS:
#    - Register a new admin user with email admin@yourdomain.com
#    - Or update an existing user to admin role in the profiles table
#    - Create test seller and customer accounts as needed
#
# 6. SAMPLE DATA (Optional):
#    - Run supabase-scripts/14-sample-data.sql for test data
#    - Or add your own products and categories
#
# ==========================================

# Payment Configuration (for future implementation)
# VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
# VITE_PAYPAL_CLIENT_ID=your_paypal_client_id

# Analytics (for future implementation)
# VITE_GOOGLE_ANALYTICS_ID=your_ga_id